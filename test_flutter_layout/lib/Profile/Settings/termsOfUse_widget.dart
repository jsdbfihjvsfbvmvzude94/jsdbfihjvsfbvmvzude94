import 'package:flutter/material.dart';

class TermsOfUseWidget extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Nutzungsbestimmungen"),
      ),
      body: Text ("Die Nutzungsbestimmungen bla bla bla bla bla ")
    );
  }
}